import {Link} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; import {LogOut} from 'lucide-react';
export default function TopNav(){const {user,logout}=useAuth();return <header className="topnav"><Link className="brand" to="/home"><span className="brand-mark">TL</span><span>TechLearn<small>Training Institute</small></span></Link><nav>{user?<><Link to="/home">Home</Link><Link to="/about">About</Link><Link to="/tasks">Tasks</Link><Link to="/attendance">Attendance</Link><button className="nav-logout" onClick={logout}><LogOut size={15}/> Logout</button></>:<><Link to="/register">Register</Link><Link className="nav-login" to="/login">Login</Link></>}</nav></header>}

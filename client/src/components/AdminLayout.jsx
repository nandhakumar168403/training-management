import {NavLink,Outlet,useNavigate} from 'react-router-dom'; import {LayoutDashboard,Users,UserRound,ClipboardCheck,BookOpen,LogOut} from 'lucide-react'; import {useAuth} from '../context/AuthContext';
const links=[['Dashboard','/admin/dashboard',LayoutDashboard],['Trainers','/admin/trainers',UserRound],['Students','/admin/students',Users],['Attendance','/admin/attendance',ClipboardCheck],['Courses & Tasks','/admin/courses',BookOpen]];
export default function AdminLayout(){const {user,logout}=useAuth();const nav=useNavigate();return <div className="admin-shell"><aside><div className="admin-brand"><span className="brand-mark">TL</span><div><b>TechLearn</b><small>ADMIN PANEL</small></div></div><div className="side-links">{links.map(([n,p,I])=><NavLink key={p} to={p}><I size={17}/>{n}</NavLink>)}</div><button className="side-logout" onClick={async()=>{await logout();nav('/admin/login')}}><LogOut size={17}/> Logout</button></aside><main className="admin-main"><div className="admin-top"><span>Management Console</span><b>Admin · {user?.username}</b></div><Outlet/></main></div>}

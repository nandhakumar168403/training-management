import {Navigate,useLocation} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; export default function ProtectedRoute({children,admin=false}){const {user,loading}=useAuth();const loc=useLocation();if(loading)return <div className="loader">Checking access…</div>;if(!user)return <Navigate to={admin?'/admin/login':'/login'} replace state={{from:loc.pathname}}/>;if(admin&&user.role!=='admin')return <Navigate to="/admin/login" replace/>;if(!admin&&user.role!=='student')return <Navigate to="/admin/dashboard" replace/>;return children}

import {useEffect,useState} from 'react'; import api from '../../services/api';
export default function Attendance(){const [items,setItems]=useState([]),[trainers,setTrainers]=useState([]),[trainerId,setTrainerId]=useState('');const load=()=>api.get('/admin/attendance',{params:trainerId?{trainerId}:{}}).then(r=>{setItems(r.data.attendance);setTrainers(r.data.trainers)});useEffect(()=>{load()},[trainerId]);const review=async(id,status)=>{await api.patch(`/admin/attendance/${id}/status`,{status});load()};return <div><div className="content-heading"><div><span className="eyebrow">MODULE 2</span><h1>Attendance Approval</h1><p>Review student attendance and approve present or absent status.</p></div><select className="select-filter" value={trainerId} onChange={e=>setTrainerId(e.target.value)}><option value="">All Trainers</option>{trainers.map(t=><option key={t._id} value={t._id}>{t.name}</option>)}</select></div><div className="panel table-wrap"><table><thead><tr><th>Student</th><th>Course</th><th>Trainer</th><th>Date</th><th>Time</th><th>Topic</th><th>Status</th><th>Action</th></tr></thead><tbody>{items.map(a=><tr key={a._id}><td><b>{a.studentId?.name}</b><small>{a.studentId?.phoneNumber}</small></td><td>{a.courseId?.name}</td><td>{a.trainerId?.name}</td><td>{new Date(a.attendanceDate).toLocaleDateString('en-IN')}</td><td>{a.classStartTime} - {a.classEndTime}</td><td>{a.topicsCovered}</td><td><span className={`status-pill ${a.status}`}>{a.status}</span></td><td className="action-group">{a.status==='pending'&&<><button className="success" onClick={()=>review(a._id,'present')}>Present</button><button className="danger" onClick={()=>review(a._id,'absent')}>Absent</button></>}</td></tr>)}</tbody></table></div></div>}

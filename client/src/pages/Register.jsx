import {useEffect,useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import api from '../services/api'; import {UserPlus,Eye,EyeOff} from 'lucide-react';
export default function Register(){const [courses,setCourses]=useState([]);const [form,setForm]=useState({name:'',email:'',phoneNumber:'',location:'',courseId:'',preferredTiming:'',joiningDate:'',password:'',confirmPassword:''});const [show,setShow]=useState(false);const [error,setError]=useState('');const [done,setDone]=useState(false);const nav=useNavigate();useEffect(()=>{api.get('/public/courses').then(r=>setCourses(r.data.courses))},[]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async e=>{e.preventDefault();setError('');if(form.password!==form.confirmPassword)return setError('Passwords do not match');try{await api.post('/auth/register',form);setDone(true);setTimeout(()=>nav('/login'),1200)}catch(e){setError(e.response?.data?.message||'Registration failed')}};return <div className="auth-page"><div className="auth-card wide"><div className="auth-icon"><UserPlus/></div><span className="eyebrow">START YOUR JOURNEY</span><h1>Create your account</h1><p className="muted">Register first. Admin approval is required before you can access the student home.</p>{done?<div className="success-box">Registration submitted successfully. Redirecting to login…</div>:<form className="form-grid" onSubmit={submit}><label>Full Name<input value={form.name} onChange={e=>set('name',e.target.value)} placeholder="Enter your full name" required/></label><label>Email<input type="email" value={form.email} onChange={e=>set('email',e.target.value)} placeholder="you@example.com" required/></label><label>Mobile Number<input value={form.phoneNumber} onChange={e=>set('phoneNumber',e.target.value)} placeholder="+91 XXXXX XXXXX" required/></label><label>Location<input value={form.location} onChange={e=>set('location',e.target.value)} placeholder="City / Location" required/></label><label>Course<select value={form.courseId} onChange={e=>set('courseId',e.target.value)} required><option value="">Select course</option>{courses.map(c=><option value={c._id} key={c._id}>{c.name}</option>)}</select></label><label>Preferred Timing<select value={form.preferredTiming} onChange={e=>set('preferredTiming',e.target.value)} required><option value="">Choose your timing</option><option>09:00 AM - 11:00 AM</option><option>11:30 AM - 01:30 PM</option><option>02:00 PM - 04:00 PM</option><option>05:00 PM - 07:00 PM</option><option>07:00 PM - 09:00 PM</option></select></label><label>Joining Date<input type="date" value={form.joiningDate} onChange={e=>set('joiningDate',e.target.value)} required/></label><label>Password<div className="password-wrap"><input type={show?'text':'password'} value={form.password} onChange={e=>set('password',e.target.value)} placeholder="Create password" required minLength="6"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><label>Confirm Password<input type="password" value={form.confirmPassword} onChange={e=>set('confirmPassword',e.target.value)} placeholder="Repeat password" required/></label>{error&&<div className="alert form-full">{error}</div>}<button className="primary form-full">Create Account</button></form>}<p className="auth-switch">Already have an account? <Link to="/login">Login</Link></p></div></div>}

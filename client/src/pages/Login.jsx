import {useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {LogIn} from 'lucide-react'; import {useAuth} from '../context/AuthContext';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const {login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{await login('/auth/login',{email,password});nav('/home')}catch(e){setError(e.response?.data?.message||'Login failed')}};return <div className="auth-page"><div className="auth-card"><div className="auth-icon"><LogIn/></div><span className="eyebrow">STUDENT ACCESS</span><h1>Welcome back</h1><p className="muted">Login after your registration has been approved by admin.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Enter your email" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" required/></label>{error&&<div className="alert">{error}</div>}<button className="primary">Login</button></form><p className="auth-switch">New student? <Link to="/register">Create account</Link></p><p className="admin-link"><Link to="/admin/login">Admin Login</Link></p></div></div>}
